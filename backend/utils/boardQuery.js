// const fs = require('fs');

// // const generateBoardId = () => {
// //   const boards = JSON.parse('./backend/db/boards.js');
// //   return Math.max(...boards.map(board => +board.boardId), 0) + 1;
// // };

// const getBoards = () => {
//   JSON.parse(fs.readFileSync('./backend/db/boards.json'));
// };

// module.exports = { getBoards };
